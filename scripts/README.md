# Scripts

Rotinas de apoio em Python. Ainda não há scripts; serão criados nas etapas próprias.

Previstos:
- `gerar_base_sintetica.py` — geração da base de validação (E16)
- extração de resultados via API do LimeSurvey (E28)

Regra: nenhum script pode processar dados pessoais reais.
