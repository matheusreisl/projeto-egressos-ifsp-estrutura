# Infraestrutura

Artefatos de provisionamento da instância LimeSurvey, definidos após o ADR-0002
(etapa E06).

Regra: o ambiente deve subir do zero usando apenas o que está versionado aqui.
Credenciais nunca entram — use arquivo `.env` local, já ignorado pelo Git.
